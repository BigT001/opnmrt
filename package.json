{
  "name": "opnmart",
  "version": "1.0.0",
  "description": "Multi-Tenant AI-Powered Storefront SaaS Platform",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test"
  }
}